# Certificate Mode Handoffsafecertificate Delegated Expansion

Intentionally invalid PF-Core adversarial fixture for **`HandoffSafeCertificate`** certificate mode.

Delegated capability exceeds source principal authority.

- **Expected error:** `HandoffAuthorityExpansion`
- **Fail stage:** `validate_handoff_authority`

Used by `check_pf_core_invalid_fixtures()` and `pcs conformance run --suite pf-core --release-grade`.
