# Certificate Mode Effectframecertificate Undeclared Effect

Intentionally invalid PF-Core adversarial fixture for **`EffectFrameCertificate`** certificate mode.

Write effect not declared in effect frame footprint.

- **Expected error:** `CapabilityEffectMismatch`
- **Fail stage:** `validate_semantics`

Used by `check_pf_core_invalid_fixtures()` and `pcs conformance run --suite pf-core --release-grade`.
