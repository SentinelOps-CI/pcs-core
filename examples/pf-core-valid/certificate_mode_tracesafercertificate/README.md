# Valid TraceSafeRCertificate fixture

Valid PF-Core trace exercising **`TraceSafeRCertificate`** certificate-mode codegen obligations.

Resource-pattern-scoped trace-safe certificate mode.

Regenerate via `python/scripts/gen_certificate_mode_fixtures.py` when certificate-mode obligations change.
