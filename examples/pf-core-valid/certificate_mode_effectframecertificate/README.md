# Valid EffectFrameCertificate fixture

Valid PF-Core trace exercising **`EffectFrameCertificate`** certificate-mode codegen obligations.

Effect frame discharge for allow events.

Regenerate via `python/scripts/gen_certificate_mode_fixtures.py` when certificate-mode obligations change.
