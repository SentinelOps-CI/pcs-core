# Valid TraceSafeCertificate fixture

Valid PF-Core trace exercising **`TraceSafeCertificate`** certificate-mode codegen obligations.

Base trace-safe certificate mode (non-tool-use).

Regenerate via `python/scripts/gen_certificate_mode_fixtures.py` when certificate-mode obligations change.
