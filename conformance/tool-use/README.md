# tool-use conformance

```bash
pcs conformance run --suite tool-use
```

Validates `examples/tool-use-release/` and asserts invalid cases under `examples/tool-use-release-invalid/` fail semantic validation.
