# computation conformance

```bash
pcs conformance run --suite computation
```

Validates `examples/computation-release/` and asserts invalid cases under `examples/computation-release-invalid/` fail semantic validation.
